# Portfolio - Vite + React + Tailwind

Quick start:
1. npm install
2. npm run dev
3. Open http://localhost:5173

Replace `src/assets/profile.jpg` with your photo. Update content in `src/pages/*` and `src/components/*`.

If you want, I can replace the placeholder with the photo you upload and regenerate the zip.
